VITE_IBMS_API_BASE_URL=http://localhost:5001/api
VITE_IBMS_APP_NAME=VANTARIS IBMS
VITE_IBMS_ENABLE_DEBUG=false

# Local smoke (FRONTEND-BACKEND-SMOKE-1):
# - Do not commit a real .env file.
# - With Vite dev, omit VITE_IBMS_API_BASE_URL to use relative /api (proxied to 127.0.0.1:5001).
# - Backend canonical entry: AN_VANTARIS_IBMS-backend/src/main.py (default port 5000 in code; use 5001 if aligned locally).
# - Protected GET endpoints return 401 without Bearer token (expected).
