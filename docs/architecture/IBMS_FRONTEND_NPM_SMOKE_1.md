# VANTARIS IBMS Frontend npm Smoke 1

## 1. Task Scope

- first frontend npm install/build/dev smoke
- frontend target only
- no raw source changed
- no backend changed
- no production API used

Target: `AN_VANTARIS_IBMS-frontend/`  
Base commit: `014f9ca` — docs(ibms): close system frontend migration batch

---

## 2. Commands Executed

```bash
cd AN_VANTARIS_IBMS-frontend
npm install
npm run build
npm run dev -- --host 127.0.0.1   # stopped after Vite ready
```

Pre-checks:

```bash
git status --short          # clean before start
find . -maxdepth 2 -type d \( -name node_modules -o -name dist \)  # none before install
```

---

## 3. Results

| Check | Result | Notes |
|---|---|---|
| npm install | PASS | 116 packages added; 2 high severity vulnerabilities reported (no audit fix in this task) |
| npm run build | PASS | `vue-tsc --noEmit && vite build` succeeded; dist generated locally (~1.1 MB JS bundle) |
| npm run dev | PASS | Vite v6.4.3 ready in 726 ms at `http://127.0.0.1:5173/`; stopped after ready |

---

## 4. Fixes Applied

| File | Change |
|---|---|
| `AN_VANTARIS_IBMS-frontend/src/services/api/system.ts` | Export shared `JsonPayload` type (`Record<string, unknown>`) required by `did.ts`, `iot.ts`, `menu.ts`, and `modeling.ts` |

Build initially failed with TS2305: `Module '"./system"' has no exported member 'JsonPayload'` in four API modules. Single-line type export resolved all four errors without new dependencies.

---

## 5. Generated Artifacts

| Artifact | Status |
|---|---|
| `package-lock.json` | Generated by `npm install`; committed |
| `node_modules/` | Generated; not committed (ignored via root `.gitignore`) |
| `dist/` | Generated by build; not committed (ignored via root `.gitignore`) |

Root `.gitignore` already includes `node_modules/` and `dist/` — no `.gitignore` change required.

---

## 6. Next Tasks

- FRONTEND-A9-EXEC-7 or DID module prep
- backend audit/notification/integration APIs
- system permission enforcement
- npm audit review (separate task)
- frontend smoke with backend running (separate task)
