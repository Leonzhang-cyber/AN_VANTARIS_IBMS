# VANTARIS IBMS Worktree npm Recovery 1

## 1. Task Scope

- clean untracked runtime artifacts
- recover frontend dependency install
- verify npm run build
- no backend source change
- no frontend source change
- no contracts change
- no raw source change

Base commit: `40809a9` — fix(ibms): return JSON error for permission DB failures

---

## 2. Initial State

| Item | Finding |
| ---- | ------- |
| git status clean | **No** — staged deletions of all 9 tracked files + ~184 untracked paths |
| Repo tracking | **Only 9 files** in git history (truncated repo after `109aba8` initial commit) |
| build failure | `vue-tsc`: `MODULE_NOT_FOUND` — `@vue/compiler-core` missing (corrupt/partial `node_modules`) |
| Junk files | `package-lock 2.json`, `package-lock 3.json` in frontend (removed) |
| Runtime dirs | `AN_VANTARIS_IBMS-frontend/node_modules`, `dist`; `AN_VANTARIS_IBMS-backend/.venv` |

**Untracked classification (summary):**

| Category | Examples | Action |
| -------- | -------- | ------ |
| node_modules / dist / .venv | frontend deps, build output, backend venv | Removed or gitignored — not committed |
| Source / docs / contracts | Full project tree on disk but not in git | **Not added** in this task — separate repo baseline recovery |
| Temp / duplicate | `package-lock 2.json`, `package-lock 3.json` | Deleted |
| Should-be-tracked docs | `docs/architecture/*`, `.gitignore` | `.gitignore` staged; recovery docs added |

---

## 3. Cleanup Actions

| Item | Action | Reason |
| ---- | ------ | ------ |
| `AN_VANTARIS_IBMS-frontend/node_modules` | Removed (then `npm ci`) | Corrupt install missing `@vue/compiler-core` |
| `AN_VANTARIS_IBMS-frontend/dist` | Removed (regenerated by build) | Runtime artifact |
| `package-lock 2.json`, `package-lock 3.json` | Deleted | Accidental duplicates |
| Staged deletions (9 tracked files) | `git reset HEAD` | Index had accidental `D` entries; working tree files intact |
| `.gitignore` | Staged for commit | Enable ignore rules for runtime artifacts |
| `AN_VANTARIS_IBMS-backend/.venv` | Left in place | Already matched ignore patterns once `.gitignore` tracked |

---

## 4. Dependency Recovery

| Item | Value |
| ---- | ----- |
| Command | **`npm ci`** (PASS) |
| Node | v20.20.2 |
| npm | 10.8.2 |
| vite | 8.0.16 |
| vue | 3.5.38 |
| vue-tsc | 2.2.12 |
| @vue/compiler-core | 3.5.38 (via `vue` → `@vue/compiler-dom` / `@vue/compiler-sfc`) |
| typescript | 5.8.3 |

---

## 5. Verification

| Check | Result | Notes |
| ----- | ------ | ----- |
| npm ci | **PASS** | 118 packages, 9s |
| npm run build | **PASS** | `vue-tsc --noEmit` + `vite build` ~12s |
| npm audit | **PASS** | 0 vulnerabilities |
| git status | **not clean** | ~184 untracked project paths remain — repo tracks only 9 files; see §6 |
| Runtime artifacts in status | **OK** | No `node_modules` / `dist` / `.venv` visible after `.gitignore` staged |

---

## 6. Files Changed

| File | Change |
| ---- | ------ |
| `.gitignore` | Staged — root ignore rules for runtime artifacts |
| `docs/architecture/IBMS_WORKTREE_NPM_RECOVERY_1.md` | Added |
| `docs/security/IBMS_WORKTREE_NPM_RECOVERY_1_RISK_REVIEW.md` | Added |

No changes to `AN_VANTARIS_IBMS-backend/src/**`, `AN_VANTARIS_IBMS-frontend/src/**`, or `contracts/**`.

**Git status note:** Full `git status --short` clean requires **REPO-BASELINE-RECOVERY** — restore full git tracking of canonical tree (currently only 9 files in history).

---

## 7. Next Tasks

- **REPO-BASELINE-RECOVERY** — restore full git index/history so `git status` is clean
- DB local-smoke execution
- approved dev JWT + DB 200 smoke
- permission enforcement
- DID module prep
